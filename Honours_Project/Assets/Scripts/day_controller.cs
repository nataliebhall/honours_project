using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class day_controller : MonoBehaviour
{
	// Use this for initialization
	void Start ()
    {
        List<day_> days_ = new List<day_>();

        // Day 1
        days_.Add(new day_());

    }
	
	// Update is called once per frame
	void Update ()
    {
		
	}
}
