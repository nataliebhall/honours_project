using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class event_ : MonoBehaviour
{
    [TextArea]
    public string text_;

    public sub_event_[] sub_;

	// Use this for initialization
	void Start ()
    {
        //foreach(options_ th in op)
        //{
        //    th
        //}
	}
	
	// Update is called once per frame
	void Update ()
    {
		
	}
}
